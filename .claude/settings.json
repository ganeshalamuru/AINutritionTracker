{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.py) cd \"C:/Users/ganes/Documents/claudecodeprojects/ai-nutrition-tracker/backend\" && uv run ruff check ;; esac",
            "statusMessage": "ruff check (backend)"
          }
        ]
      }
    ]
  }
}
